---
layout: default
---
<h1>{{ page.title }}</h1>

<p>
    {{ page.date | date: "%B %Y" }}
</p>

{{ content }}
{% if page.draft_readers %}
<div>
  <b>Thanks</b> to  {{ page.draft_readers }} for reading drafts of this.
</div>
{% endif %}
<a id="logged">
    essay {% if page.time_logged %}took {{ page.time_logged }}{% endif %}
</a>
